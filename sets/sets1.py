#Sets are the collection of the unique elements where sets are mutable 
#and elements inside the sets are immutable.
# And we can not write the double elements inside the set.

s={1,2,2,3,3,5}

print(type(s))

#We can also add elements in sets like:

s.add(9)
print(s)

# for empty the Method is:

empty_set=set()
print(type(empty_set))